import { DashboardWidget } from './DashboardWidget';

/** Stub widget for the treasury balance overview. Implemented in a later ticket. */
export function TreasuryPulseHeader() {
  return <DashboardWidget title="Treasury Pulse" />;
}
