import { DashboardWidget } from './DashboardWidget';

/** Stub widget for the batch refill control panel. Implemented in a later ticket. */
export function BatchRefillModule() {
  return <DashboardWidget title="Batch Refill" />;
}
