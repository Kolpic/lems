import { DashboardWidget } from './DashboardWidget';

/** Stub widget for the global transaction feed. Implemented in a later ticket. */
export function GlobalTransactionFeed() {
  return <DashboardWidget title="Global Transaction Feed" />;
}
